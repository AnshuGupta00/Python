    # function
    # Set of code that perfoms a sepecific task.
    # it allows you to modularize your code and reuse it in different parts of your program.
    # write clean code and make it easier to read and maintain.
    #It has tWo types of function:
    # 1. Built-in functions
        #The function which are coming with python are called built-in functions.
        #Print(), len(), type(), input(), range() are some of the built-in functions in python.
    #2. User-defined functions
        #The function which are defined by the user are called user-defined functions.
        #explicitly defined by the user to perform a specific task.
    
    #3.How to create a function in python ?
    # Syntax: def function_name(parameters):
    #           # code block
    #           # return value
    #calling a function  : function_name()